# Pool

Package `pool` provides smart Object Pools with **Self-Calibration**: automatically learns the most common sizes and discards outliers, enabling more efficient memory reuse than Go's standard `sync.Pool`.
